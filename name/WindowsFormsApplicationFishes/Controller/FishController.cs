using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

using WindowsFormsApplicationFishes.Model;

namespace WindowsFormsApplicationFishes.Controller
{
    class FishController
    {
        SmallFish[,] fishMatrix;
        Rectangle[,] rectangleMatrix;

        SmallFish currentFish;
        Rectangle currentFishRectangle;

        public void find(int a)
        {
            //currentFish = null;
            //if (a >= sea.GetLength(1))
            //    return;
            //for (int i = sea.GetLength(0) - 1; i >= 0; i--)
            //{
            //    if (sea[i, a] != null)
            //    {
            //        currentFish = sea[i, a];
            //        sea[i, a] = null;
            //        break;
            //    }
            //}
        }
    }
}
